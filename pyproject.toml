# pyproject.toml

[build-system]
requires = ["setuptools>=67.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "steam-api-collector"
version = "1.0.0"
dependencies = [
    "pandas",
    "pyarrow",
    "sqlalchemy",
    "mysql-connector-python"
]